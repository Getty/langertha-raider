name    = Langertha-Raider
author  = Torsten Raudssus <torsten@raudssus.de> L<https://raudssus.de/>
license = Perl_5
copyright_holder = Torsten Raudssus
copyright_year   = 2026
main_module = lib/Langertha/Raider.pm

; Getty release command:
;   dzil release
; run_after_release publishes a GitHub release and pushes the Docker
; Hub image (see maint/release-after.pl for the repo/image defaults).
; Override gh/docker via GH_BIN / DOCKER_BIN.
; Extra docker build args:
;   RAIDER_DOCKER_BUILD_ARGS='--platform linux/amd64,linux/arm64' dzil release

[@Author::GETTY]
irc = #langertha
irc_user = Getty
; Git::GatherDir ships every tracked file, so development infrastructure has to be
; excluded explicitly or it silently joins the CPAN tarball.
gather_exclude_match = ^\.claude/
gather_exclude_match = ^CLAUDE\.md$
gather_exclude_match = ^TODO\.md$
gather_exclude_match = ^MIGRATION-FROM-LANGERTHA\.md$
gather_exclude_match = ^STATE-AND-VISION\.md$
run_after_release = %x %o/maint/release-after.pl --archive %a --dir %d --version %v
